package org.example.semscan.ui.teacher;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

/**
 * Legacy placeholder. Availability management now lives on the web console,
 * so this activity simply exits immediately if launched.
 */
@Deprecated
public class AddAvailabilityActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        finish();
    }
}
